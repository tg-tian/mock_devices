export const propertiesTopic = (deviceId: string) => `devices/${deviceId}/property`
export const eventsTopic = (deviceId: string) => `devices/${deviceId}/events`
export const commandsTopic = (deviceId: string) => `devices/${deviceId}/command`
export const configTopic = (deviceId: string) => `devices/${deviceId}/config`
